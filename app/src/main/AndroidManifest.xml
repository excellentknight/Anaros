<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.faresimtiez.algotech.anaros">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ServicesListActivity"
            android:label="@string/title_activity_services_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Service1CardActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ClientRDVActivity"
            android:label="@string/title_activity_client_rdv"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CommandeActivity" />
        <activity
            android:name=".AdminLoginActivity"
            android:label="@string/title_activity_admin_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdminMainActivity"
            android:label="@string/title_activity_admin_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdminRDVActivityActivity"
            android:label="@string/title_activity_admin_rdvactivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CheckComandeActivity" />
        <activity
            android:name=".AdminServicesListActivity"
            android:label="@string/title_activity_admin_services_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminService1CardActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".SalonActivity"
            android:label="@string/title_activity_salon"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DevActivity"
            android:label="@string/title_activity_dev"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".WebActivity"
            android:label="@string/title_activity_web"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ContactActivity"
            android:label="@string/title_activity_contact"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EmailActivity"
            android:label="@string/title_activity_email"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ShareExActivity"
            android:label="@string/title_activity_share_ex"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Service1CommandeActivity"
            android:label="@string/title_activity_service1_commande"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ServicesCardActivity"
            android:label="@string/title_activity_services_card"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ClientRDVm2Activity"
            android:label="@string/title_activity_client_rdvm2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CommandeM2Activity"></activity>
    </application>

</manifest>